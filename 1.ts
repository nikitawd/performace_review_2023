interface User {
  name: string;
  age: number;
}

const user = {
  name: "Alex",
  age: 24,
};

const favoriteCars = ["Ford", "Toyota", "Honda"];

// * 1. write a function which accepts user and returns 'Hi, {name}, you are {age} years old'

// * 2. write a function which accepts favoriteCars and returns third car
